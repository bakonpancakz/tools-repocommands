{
  "private": true,
  "version": "1.0.0",
  "name": "repo-commands",
  "main": "./src/Service.ts",
  "scripts": {
    "start": "npm install && ts-node .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.1",
    "ts-node": "^10.9.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^18.0.6",
    "nodemon": "^2.0.19"
  }
}